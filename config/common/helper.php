<?php

//辅助函数

use config\lib\conf;
use think\Config;

if (!function_exists('config')) {
    /**
     * 获取和设置配置参数
     * @param string|array  $name 名称
     * @param mixed         $file 文件
     * @return mixed
     */
    function config($name = '', $file = null)
    {

    }
}